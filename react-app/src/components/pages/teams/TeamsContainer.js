import TeamsList from "./TeamsList";

const TeamsContainer = () => {
    return (
        <div>
            <TeamsList />
            <TeamsList />
            <TeamsList />
        </div>
    )
};

export default TeamsContainer;
